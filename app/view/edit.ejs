<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Laporan - CampusFix</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-5 mb-5">
    <div class="card shadow">
        <div class="card-header bg-warning text-dark">
            <h3 class="mb-0">✏️ Edit Laporan</h3>
        </div>
        
        <div class="card-body">
            <form action="/update/<%= laporan.id %>" method="POST">
                
                <div class="mb-3">
                    <label class="form-label fw-bold">Kategori Kerusakan</label>
                    <select name="id_kategori" class="form-select" required>
                        <% kategori.forEach(item => { %>
                            <option value="<%= item.id %>" <%= item.id === laporan.id_kategori ? 'selected' : '' %>>
                                <%= item.nama_kategori %>
                            </option>
                        <% }) %>
                    </select>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold">Judul Masalah</label>
                    <input type="text" class="form-control" name="judul" value="<%= laporan.judul %>" required>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold">Lokasi</label>
                    <input type="text" class="form-control" name="lokasi" value="<%= laporan.lokasi %>" required>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold">Deskripsi</label>
                    <textarea class="form-control" name="deskripsi" rows="3" required><%= laporan.deskripsi %></textarea>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold">Status Pengerjaan</label>
                    <select name="status" class="form-select bg-warning-subtle border-warning">
                        <option value="Pending" <%= laporan.status === 'Pending' ? 'selected' : '' %>>⏳ Pending (Menunggu)</option>
                        <option value="Proses" <%= laporan.status === 'Proses' ? 'selected' : '' %>>🔧 Proses (Sedang Dikerjakan)</option>
                        <option value="Selesai" <%= laporan.status === 'Selesai' ? 'selected' : '' %>>✅ Selesai (Sudah Diperbaiki)</option>
                    </select>
                </div>

                <hr>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary">Simpan Perubahan</button>
                    <a href="/" class="btn btn-secondary">Batal</a>
                </div>

            </form>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>