<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen font-sans">

    <nav class="bg-blue-600 text-white p-4 shadow-lg">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-bold">🛠️ CampusFix</h1>
            <span class="text-sm bg-blue-700 px-3 py-1 rounded">Kelompok C3</span>
        </div>
    </nav>

    <div class="container mx-auto mt-10 p-4">
        
        <div class="flex justify-between items-center mb-6">
            <h2 class="text-xl font-semibold text-gray-700">Daftar Laporan Masuk</h2>
            <a href="/tambah" class="bg-green-500 hover:bg-green-600 text-white px-4 py-2 rounded shadow transition">
                + Buat Laporan Baru
            </a>
        </div>

        <div class="bg-white rounded-lg shadow overflow-hidden">
            <div class="overflow-x-auto">
                <table class="w-full text-left border-collapse">
                    <thead class="bg-gray-800 text-white">
                        <tr>
                            <th class="p-3">No</th>
                            <th class="p-3">Tanggal</th>
                            <th class="p-3">Kategori</th>
                            <th class="p-3">Judul Masalah</th>
                            <th class="p-3">Lokasi</th>
                            <th class="p-3">Status</th>
                            <th class="p-3 text-center">Aksi</th>
                        </tr>
                    </thead>
                    <tbody class="text-gray-600">
                        <% if (laporan.length === 0) { %>
                            <tr>
                                <td colspan="7" class="p-6 text-center text-gray-500 font-semibold">
                                    Belum ada laporan masuk. Yuk laporin kerusakan!
                                </td>
                            </tr>
                        <% } else { %>
                            <% laporan.forEach((item, index) => { %>
                                <tr class="border-b hover:bg-gray-50 transition">
                                    <td class="p-3"><%= index + 1 %></td>
                                    
                                    <td class="p-3 text-sm">
                                        <%= new Date(item.created_at).toLocaleDateString('id-ID') %>
                                    </td>
                                    
                                    <td class="p-3">
                                        <span class="bg-blue-100 text-blue-800 text-xs font-medium px-2.5 py-0.5 rounded">
                                            <%= item.nama_kategori || 'Tanpa Kategori' %>
                                        </span>
                                    </td>

                                    <td class="p-3 font-medium text-gray-800"><%= item.judul %></td>
                                    <td class="p-3"><%= item.lokasi %></td>
                                    
                                    <td class="p-3">
                                        <% if (item.status === 'Selesai') { %>
                                            <span class="bg-green-100 text-green-800 text-xs font-bold px-2.5 py-0.5 rounded border border-green-400">
                                                ✅ Selesai
                                            </span>
                                        <% } else if (item.status === 'Proses') { %>
                                            <span class="bg-blue-100 text-blue-800 text-xs font-bold px-2.5 py-0.5 rounded border border-blue-400">
                                                🔧 Proses
                                            </span>
                                        <% } else { %>
                                            <span class="bg-yellow-100 text-yellow-800 text-xs font-bold px-2.5 py-0.5 rounded border border-yellow-400">
                                                ⏳ Pending
                                            </span>
                                        <% } %>
                                    </td>

                                    <td class="p-3 text-center space-x-1">
                                        <a href="/edit/<%= item.id %>" class="bg-blue-500 hover:bg-blue-600 text-white px-3 py-1 rounded text-xs shadow transition">
                                            Edit
                                        </a>
                                        <a href="/hapus/<%= item.id %>" class="bg-red-500 hover:bg-red-600 text-white px-3 py-1 rounded text-xs shadow transition" onclick="return confirm('Yakin mau menghapus laporan ini?')">
                                            Hapus
                                        </a>
                                    </td>
                                </tr>
                            <% }) %>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
        
    </div>

    <footer class="text-center py-6 text-gray-500 text-sm">
        &copy; 2024 Kelompok C3 - Praktikum Tekser
    </footer>

</body>
</html>